<% @page_title = "Edit Page" %>

<%= link_to("<< Back to List", subject_pages_path(@subject.id)) %>

<div class="pages edit">
  <h2>Update Page</h2>

  <%= form_for([@subject, @page]) do |f| %>

      <%= render(:partial => "form", :locals => {:f => f}) %>

      <div class="form-buttons">
        <%= submit_tag("Update Page") %>
      </div>

  <% end %>
</div>
